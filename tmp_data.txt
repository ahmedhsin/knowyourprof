admins = [
    Admin(email='admin1@example.com', name='Admin 1', password='admin_password1', verified=True),
    Admin(email='admin2@example.com', name='Admin 2', password='admin_password2', verified=False)
]

profs = [
    Prof(name='Ahmed Salah', admin=admins[0]),
    Prof(name='Sara Khan', admin=admins[1]),
    Prof(name='John Smith', admin=admins[0])
]

users = [
    User(email='user1@example.com', name='User 1', password='user_password1'),
    User(email='user2@example.com', name='User 2', password='user_password2'),
    User(email='user3@example.com', name='User 3', password='user_password3')
]

reviews = [
    Review(text='Great professor!', rating=4, user=users[0], prof=profs[0], admin=admins[0]),
    Review(text='Needs improvement.', rating=2, user=users[1], prof=profs[1], admin=admins[1]),
    Review(text='Very knowledgeable.', rating=5, user=users[2], prof=profs[2], admin=admins[0])
]

reactions = [
    Reaction(react=True, user=users[0], review=reviews[0]),
    Reaction(react=False, user=users[1], review=reviews[1]),
    Reaction(react=True, user=users[2], review=reviews[2])
]

facilities = [
    Facility(name='Lab'),
    Facility(name='Library'),
    Facility(name='Classroom')
]

# Add relationships
profs[0].facilities.extend(facilities)
admins[0].approved_profs.extend(profs)
admins[1].approved_profs.append(profs[1])
for i in range(3):
    users[i].reviews.append(reviews[i])
    users[i].reacts.append(reactions[i])
    reviews[i].reacts.append(reactions[i])

# Commit changes to the database
db.session.add_all(admins + profs + users + reviews + reactions + facilities)
db.session.commit()